using UnityEngine;

/// <summary>
/// This script is currently a placeholder or can be used for Flow State specific visual effects.
/// The core logic is integrated into TypingSystem.cs.
/// </summary>
public class FlowState : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
